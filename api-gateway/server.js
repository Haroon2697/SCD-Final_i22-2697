const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;quire('http-proxy-middleware');
');
app.use(express.json());v');
const cors = require('cors');
app.listen(PORT, () => {
    console.log(`API Gateway running on port ${PORT}`);dotenv.config();
});});